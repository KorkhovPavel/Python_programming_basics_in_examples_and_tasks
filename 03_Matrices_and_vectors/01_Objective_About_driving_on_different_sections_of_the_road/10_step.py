import numpy as np

a = np.array([2, 5, 2, 0, 0])

b = np.array([5, -1, -1, 6, 2])


# c = np.where(a == b)[0]
# print(c)

# c = np.where(np.logical_and(a > 1, b < 0))[0]
# print(c)
# c = np.where(np.logical_or(a >= 0, b == 6))[0]
# print(c)
# c = np.where(np.logical_not(b <= 0))[0]
# print(c)
# c = np.where(a == 0)[0]
# print(c)