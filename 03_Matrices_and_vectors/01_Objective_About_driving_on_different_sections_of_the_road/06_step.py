import numpy as np

a = np.array([2, 5, -2, 0, 8])

b = np.array([5, -1, -3, 6, 2])

print(a + b)

print(np.sum(np.cos(b)))

print(np.sum(a) + np.mean(a))

print(np.round(a, 2))

print(a + np.sum(b))

print(np.pi - a)
